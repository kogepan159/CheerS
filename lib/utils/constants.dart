enum PostCaptionOpenMode {
  FROM_POST,
  FROM_PROFILE,
}

enum FeedMode {

  FROM_FEED,
  FROM_PROFILE,

}

enum HostPartyMenu{
  EDIT,
  DELETE,
}

enum ProfileMode{
  MYSELF,
  OTHER,

}

enum ProfileSettingMenu{
  THEME_CHANGE,
  SIGN_OUT,

}

enum NumberOfFriendsScreenOpenMode{
  FROM_HostParty,
  FROM_PROFILE,

}

enum ChatMenu{
  DELETE,
}